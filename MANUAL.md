# OfficeMonitor 사용 매뉴얼

## 목차

1. [설치](#1-설치)
2. [첫 실행](#2-첫-실행)
3. [화면 구성](#3-화면-구성)
4. [모니터링](#4-모니터링)
5. [방문자 관리](#5-방문자-관리)
6. [통계](#6-통계)
7. [녹화와 캡처](#7-녹화와-캡처)
8. [설정](#8-설정)
9. [시스템 트레이](#9-시스템-트레이)
10. [단축키 모음](#10-단축키-모음)
11. [문제 해결](#11-문제-해결)

---

## 1. 설치

### 요구 사항

| 항목 | 조건 |
|------|------|
| OS | Windows 10/11 |
| Python | 3.10 이상 |
| 카메라 | USB 웹캠 (DirectShow 지원) |
| 저장 공간 | 최소 2GB 여유 (모델 다운로드 + 데이터) |

### 설치 순서

**1단계: 저장소 클론**

```bash
git clone https://github.com/yijae78/Office-Monitor.git
cd Office-Monitor
```

**2단계: 가상환경 생성 (권장)**

```bash
python -m venv .venv
.venv\Scripts\activate
```

**3단계: 의존성 설치**

```bash
pip install -r requirements.txt
```

> **설치 실패 시:** `insightface`나 `onnxruntime` 패키지에서 에러가 발생하면 [Visual C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)를 먼저 설치한 후 다시 시도하세요. 설치 시 "C++를 사용한 데스크톱 개발" 워크로드를 선택합니다.

**4단계: 실행**

```bash
python main.py
```

> 최초 실행 시 InsightFace 모델(`buffalo_l`, 약 300MB)과 YOLO11n 모델이 자동 다운로드됩니다. 인터넷 연결이 필요하며 1~5분 소요될 수 있습니다.

---

## 2. 첫 실행

### 자동으로 생성되는 것들

앱이 처음 실행되면 다음 폴더와 파일이 자동 생성됩니다:

```
C:\OfficeMonitor\              ← 기본 데이터 저장 경로
├── data\
│   ├── monitor.db             ← SQLite 데이터베이스
│   ├── thumbnails\            ← 방문 썸네일 이미지
│   └── pending_faces\         ← 미등록 얼굴 캡처 이미지
├── snapshots\                 ← 스냅샷 이미지
├── recordings\                ← 녹화 영상
└── app.log                    ← 앱 로그
```

> 데이터 저장 경로는 `config.yaml`의 `storage.data_dir`에서 변경할 수 있습니다.

### 카메라 연결

- 앱이 시작되면 자동으로 카메라를 찾아 연결합니다.
- 기본 카메라(ID 0)가 실패하면 대체 ID(2, 3)를 순서대로 시도합니다.
- 가상 카메라(화면 캡처 소프트웨어 등)는 자동으로 건너뜁니다.
- 연결 상태는 우측 패널의 **카메라 정보** 카드에서 확인할 수 있습니다.

### 데모 데이터로 체험하기

기능을 먼저 둘러보고 싶다면 데모 데이터를 생성할 수 있습니다:

1. 상단 **⚙ 설정** 버튼 클릭
2. 맨 아래 **데모 모드** 섹션에서 **데모 생성** 클릭
3. 방문자관리, 통계 탭에서 샘플 데이터 확인
4. 체험 후 **데모 삭제**로 정리 (실제 데이터는 보존됨)

---

## 3. 화면 구성

### 전체 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  ● OfficeMonitor   [모니터링] [방문자관리] [통계]   시간  ↻ ⚙  │  ← 헤더바
├─────────────────────────────────────────────────────────┤
│  오늘 방문자: 5    감지 중: 2    녹화 시간: 01:23    용량: 1.2GB  │  ← KPI 카드
├─────────────────────────────────────────────────────────┤
│  카메라 ▼  ● 녹화  ⏸   📷   − 1.0× + 1:1   ⛶             │  ← 툴바
├─────────────────────────────────────┬───────────────────┤
│                                     │ 카메라 정보       │
│                                     │ 감지 상태         │
│           카메라 영상                │ 녹화              │
│                                     │ 오늘 방문자       │
│                                     │  (타임라인)        │
├─────────────────────────────────────┴───────────────────┤
│  상태 메시지                                              │  ← 상태바
└─────────────────────────────────────────────────────────┘
```

### 헤더바

| 요소 | 설명 |
|------|------|
| 세그먼트 버튼 | **모니터링** / **방문자관리** / **통계** 탭 전환 |
| 시계 | 현재 날짜와 시간 (1초 갱신) |
| **↻** 새로고침 | 엔진 재시작 + 코드 핫리로드 |
| **⚙** 설정 | 설정 다이얼로그 열기 |

### KPI 카드

실시간으로 갱신되는 4개의 요약 지표입니다 (5초 간격).

| 카드 | 내용 |
|------|------|
| 오늘 방문자 | 오늘 감지된 총 방문자 수 |
| 감지 중 | 현재 화면에서 감지되고 있는 사람 수 |
| 녹화 시간 | 현재 녹화 경과 시간 |
| 저장 용량 | 데이터 폴더 전체 용량 |

### 상단 패널 접기

**▲** 버튼을 클릭하면 헤더바 + KPI + 툴바를 모두 접어서 카메라 영상을 최대화할 수 있습니다. 다시 **▼**를 클릭하면 펼쳐집니다.

### 우측 패널

카메라 영상 오른쪽의 정보 패널입니다.

- **◀ / ▶** 버튼으로 접기/펼치기 가능
- 창 너비 700px 미만이면 자동으로 숨겨짐
- 각 섹션은 **▼ / ▶**로 개별 접기 가능

---

## 4. 모니터링

### 화면 표시 (바운딩 박스)

카메라 영상 위에 감지된 사람이 색상 박스로 표시됩니다.

| 표시 | 의미 |
|------|------|
| **초록 실선** + 이름 | 등록된 방문자 (얼굴 매칭 성공) |
| **초록 점선** + 이름 | 등록된 방문자 (뒷모습 — 이전 매칭으로 추적 유지) |
| **빨간 실선** + "미등록" | 얼굴이 보이지만 등록되지 않은 사람 |
| **빨간 점선** + "미인식" | 사람은 감지되었으나 얼굴이 안 보이는 경우 |

### 감지 파이프라인

```
카메라 프레임 → YOLO11n (사람 감지) → ByteTrack (추적 ID 부여)
                                         ↓
                              얼굴 보임 → InsightFace 임베딩 매칭
                              얼굴 안 보임 → 기존 추적 ID의 이름 유지
```

- 한 번 이름이 바인딩된 추적 ID는 그 사람이 화면을 벗어날 때까지 유지됩니다.
- 뒤돌아서 얼굴이 안 보여도 추적이 끊기지 않습니다.

### 줌 (확대/축소)

| 조작 | 동작 |
|------|------|
| **+** 버튼 | 0.25 단위 확대 |
| **−** 버튼 | 0.25 단위 축소 |
| **1:1** 버튼 | 원래 크기로 복귀 |
| **Ctrl + 마우스 휠** | 부드러운 줌 |

- 줌 범위: 0.5배 ~ 3.0배
- 확대 시 화면 중앙을 기준으로 크롭됩니다.

### 전체화면

- **⛶** 버튼 또는 **F11** 키로 전체화면 전환
- **ESC** 또는 다시 **F11**로 해제

### 미등록자 자동 캡처

등록되지 않은 사람이 감지되면:

1. 3초간 최고 품질 프레임을 자동 선별
2. 얼굴 이미지를 `pending_faces/` 폴더에 저장
3. 하단 상태바에 "미등록 얼굴 자동 캡처" 메시지 표시
4. **방문자관리** 탭의 **미등록 캡처**에서 확인 가능

### 방문 기록 (타임라인)

새로운 방문자가 감지되면:

- 우측 패널 **오늘 방문자** 타임라인에 자동 추가
- 토스트 알림 표시 (등록자: 초록, 미등록자: 빨강)
- 미등록자 감지 시 경고음 발생
- 동일인은 쿨다운 시간(기본 5분) 내 중복 기록되지 않음

### 타임라인 항목 클릭

타임라인의 방문 기록을 클릭하면 **썸네일 팝업**이 나타납니다.

- 감지 당시의 얼굴 사진을 크게 볼 수 있음
- **미등록 방문자**의 경우 팝업에서 바로:
  - **방문자로 등록** — 이름 입력 후 등록 (이후 자동 인식)
  - **삭제** — 해당 방문 기록 삭제

### 오늘 기록 리셋

우측 패널의 **초기화** 버튼 또는 감지 상태 카드의 **오늘 기록 리셋** 버튼:

- 오늘의 방문 기록, 타임라인, 미등록 캡처를 모두 초기화
- 확인 다이얼로그가 표시됨

---

## 5. 방문자 관리

상단 세그먼트에서 **방문자관리** 탭을 선택합니다.

### 4개 하위 탭

| 탭 | 설명 |
|----|------|
| **미등록 캡처** | 자동 캡처된 미등록 얼굴 목록 |
| **등록된 방문자** | 현재 등록된 방문자 관리 |
| **삭제된 방문자** | 삭제 후 복구 가능한 방문자 |
| **휴지통** | 삭제된 캡처 이미지 |

### 미등록 캡처 탭

자동으로 캡처된 미등록 얼굴이 카드 형태로 나열됩니다.

각 카드에서:
- **등록** 버튼 → 이름 입력 → 방문자로 등록 (이후 자동 인식됨)
- **삭제** 버튼 → 휴지통으로 이동

### 등록된 방문자 탭

**뷰 모드 전환:**
- **≡ 리스트** — 한 줄에 하나씩, 컴팩트하게
- **⊞ 그리드** — 카드 형태로 넓게

**검색:** 상단 검색창에 이름 입력 시 실시간 필터링

각 방문자에서:
- **이름변경** — 새 이름 입력
- **삭제** — 삭제된 방문자 탭으로 이동 (임베딩 포함)

**표시 정보:**
- 얼굴 썸네일
- 이름
- 임베딩 수 (최대 10개 — 다각도 학습 데이터)

### 삭제된 방문자 탭

삭제된 방문자를 복구하거나 영구 삭제할 수 있습니다.

- **복구** — 등록 상태로 되돌림 (임베딩도 복원)
- **영구삭제** — 완전 삭제 (되돌릴 수 없음)
- **전체삭제** — 삭제된 방문자 전원 영구 삭제

### 휴지통 탭

삭제된 캡처 이미지가 보관됩니다.

- 체크박스로 여러 개 선택 가능
- **전체선택 / 선택해제** 토글
- **선택 삭제** — 체크된 항목만 영구 삭제
- **전체 비우기** — 휴지통 완전 비우기
- 개별 카드에서 **복구** 또는 **삭제** 가능

### 이미지에서 방문자 등록

사진 파일로 방문자를 직접 등록할 수 있습니다.

1. 상단의 **+ 이미지에서 등록** 버튼 클릭
2. 이미지 파일 선택 (jpg, png, bmp 지원)
3. 자동으로 얼굴 검출 및 임베딩 추출
4. 이름 입력 후 등록 완료

> 사진에 얼굴이 명확하게 보여야 합니다. 얼굴이 감지되지 않으면 오류 메시지가 표시됩니다.

### 임베딩 자동 수집

등록된 방문자가 다시 감지되면:

- 다른 각도의 얼굴이면 자동으로 임베딩을 추가 학습
- 방문자당 최대 10개의 다각도 임베딩을 수집
- 다양한 각도를 학습할수록 인식 정확도가 향상됨
- 설정에서 `자동 임베딩 보강` 끄기 가능

---

## 6. 통계

상단 세그먼트에서 **통계** 탭을 선택합니다.

### 기간 선택

우측 상단 드롭다운에서:
- **최근 7일** (기본)
- **최근 14일**
- **최근 30일**

### 달력으로 특정 날짜 보기

좌측 달력에서 날짜를 클릭하면 해당 날짜의 상세 통계를 볼 수 있습니다.

- **날짜 선택 해제** 버튼으로 기간 모드로 복귀

### 통계 내용

| 항목 | 설명 |
|------|------|
| 요약 카드 | 총 방문 / 등록 방문자 / 미등록 방문자 수 |
| 일별 방문자 수 | 막대 차트 (선택 기간 기준) |
| 시간대별 분포 | 막대 차트 (오늘 또는 선택 날짜) |
| TOP 5 방문자 | 기간 모드: 가장 자주 방문한 5명 |
| 날짜별 기록 | 날짜 모드: 해당 날짜의 전체 방문 목록 |

### CSV 내보내기

**📥 CSV 내보내기** 버튼으로 방문 기록을 CSV 파일로 저장할 수 있습니다.

내보내기 항목: ID, 방문자ID, 이름, 시간, 신뢰도, 등록 여부

---

## 7. 녹화와 캡처

### 녹화

| 버튼 | 동작 |
|------|------|
| **● 녹화** | 녹화 시작 (버튼이 "■ 중지"로 변경) |
| **■ 중지** | 녹화 중지 및 파일 저장 |
| **⏸ 일시정지** | 녹화 일시정지 (버튼이 "▶ 재개"로 변경) |
| **▶ 재개** | 일시정지 해제 |

**녹화 파일:**
- 저장 위치: `C:\OfficeMonitor\recordings\`
- 파일명: `rec_YYYYMMDD_HHMMSS.avi`
- 자동 분할: 설정된 시간(기본 30분)마다 새 파일 생성
- 코덱: XVID (기본)

**우측 패널 녹화 카드에서:**
- 현재 녹화 상태 및 경과 시간 확인
- **📂 폴더** — 탐색기에서 녹화 폴더 열기
- **🗑 전체삭제** — 모든 녹화 파일 삭제
- 최근 10개 녹화 파일 목록 (클릭하면 재생)

### 스냅샷 캡처

**📷 캡처** 버튼 또는 **Ctrl+Shift+C**를 누르면:

1. 화면에 "캡처할 영역을 드래그하세요" 안내 표시
2. 마우스로 원하는 영역을 드래그하여 선택
3. 선택 영역의 크기가 실시간으로 표시됨
4. 드래그 완료 시 해당 영역만 캡처
5. "방문자로 등록하시겠습니까?" 확인 팝업 표시

> 드래그 없이 클릭만 하면 전체 화면이 캡처됩니다.
> **ESC** 키로 캡처를 취소할 수 있습니다.

**캡처 후 방문자 등록:**
- 확인 팝업에서 **Yes** → 이름 입력 → 캡처한 이미지에서 얼굴 검출 → 등록

---

## 8. 설정

상단 **⚙** 버튼으로 설정 다이얼로그를 열 수 있습니다.

### 카메라

| 항목 | 설명 | 기본값 |
|------|------|--------|
| 카메라 ID | 사용할 카메라 장치 번호 | 0 |
| 해상도 (가로) | 카메라 가로 해상도 | 1280 |
| 해상도 (세로) | 카메라 세로 해상도 | 720 |

### 얼굴 감지

| 항목 | 설명 | 기본값 | 범위 |
|------|------|--------|------|
| 감지 간격 | 얼굴 감지 주기 (낮을수록 빠르지만 CPU 부하 증가) | 200ms | 50~2000ms |
| 감지 임계값 | 얼굴 존재 판단 신뢰도 | 0.35 | 0.1~1.0 |
| 유사도 임계값 | 등록 얼굴과 비교 시 매칭 기준 (낮을수록 관대) | 0.65 | 0.1~1.0 |
| 쿨다운 | 같은 사람 재기록 방지 시간 | 300초 | 10~3600초 |
| 자동 임베딩 보강 | 다각도 임베딩 자동 수집 여부 | ON | - |

> **유사도 임계값 조정 팁:** 인식이 잘 안 되면 0.55~0.60으로 낮추고, 다른 사람으로 오인식되면 0.70~0.75로 높이세요.

### 녹화

| 항목 | 설명 | 기본값 |
|------|------|--------|
| FPS | 녹화 프레임 속도 | 15 |
| 분할 단위 | 자동 파일 분할 시간 | 30분 |
| 자동 녹화 시작 | 앱 시작 시 자동 녹화 | OFF |

### 저장

| 항목 | 설명 | 기본값 |
|------|------|--------|
| 데이터 보존 | 오래된 데이터 자동 삭제 기간 | 3일 |
| 정리 주기 | 자동 정리 실행 간격 | 1시간 |

### 데모 모드

| 버튼 | 동작 |
|------|------|
| **데모 생성** | 샘플 방문자 + 방문 기록 생성 |
| **데모 삭제** | 데모 데이터만 삭제 (실제 데이터 보존) |

설정 변경 후 **저장** 버튼을 누르면 `config.yaml`에 기록됩니다. 일부 설정은 **↻ 새로고침** 후 적용됩니다.

---

## 9. 시스템 트레이

- 앱을 최소화하면 작업표시줄에 유지됩니다.
- 시스템 트레이 아이콘을 **더블클릭**하면 창이 복원됩니다.
- 트레이 아이콘 **우클릭** 메뉴:
  - **열기** — 창 복원
  - **종료** — 앱 완전 종료

---

## 10. 단축키 모음

| 단축키 | 기능 |
|--------|------|
| `Ctrl+Shift+C` | 영역 캡처 |
| `Ctrl+R` | 녹화 시작/중지 |
| `Ctrl+P` | 녹화 일시정지/재개 |
| `Ctrl+마우스 휠` | 카메라 줌 |
| `F11` | 전체화면 전환 |
| `ESC` | 영역 선택 취소 / 전체화면 해제 |

---

## 11. 문제 해결

### 카메라가 연결되지 않음

**증상:** "카메라를 찾을 수 없습니다" 메시지

**해결:**
1. 웹캠이 USB에 제대로 연결되었는지 확인
2. 다른 프로그램(Zoom, Teams 등)이 카메라를 사용 중인지 확인 → 해당 프로그램 종료
3. 설정에서 **카메라 ID**를 변경해보기 (0, 1, 2 등)
4. 장치 관리자에서 카메라 드라이버 상태 확인

### 얼굴 인식률이 낮음

**해결:**
1. 카메라 해상도를 높이기 (1280x720 이상 권장)
2. 조명을 개선하기 (얼굴에 그림자가 지지 않도록)
3. **유사도 임계값**을 0.55~0.60으로 낮추기
4. 여러 각도에서 얼굴을 보여주어 임베딩을 추가 학습시키기
5. **이미지에서 등록** 기능으로 정면 사진을 직접 등록하기

### 다른 사람으로 잘못 인식됨

**해결:**
1. **유사도 임계값**을 0.70~0.75로 높이기
2. 해당 방문자를 삭제 후 더 선명한 사진으로 재등록하기

### 앱이 느리거나 CPU 사용량이 높음

**해결:**
1. **감지 간격**을 300~500ms로 높이기 (기본 200ms)
2. 카메라 해상도를 낮추기 (640x480)
3. 다른 무거운 프로그램 종료하기
4. 백그라운드 녹화를 중지하기

### pip install 실패 (insightface / onnxruntime)

**증상:** `pip install -r requirements.txt`에서 빌드 에러 발생

**해결:**
1. [Visual C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/) 설치
2. 설치 시 **"C++를 사용한 데스크톱 개발"** 워크로드 선택
3. 설치 완료 후 터미널을 재시작하고 `pip install -r requirements.txt` 재시도

### 모델 다운로드 실패

**증상:** 첫 실행 시 오류 발생

**해결:**
1. 인터넷 연결 확인
2. 방화벽/프록시 설정 확인
3. 수동 재시도: 앱을 종료 후 다시 실행
4. InsightFace 모델이 `~/.insightface/` 폴더에 있는지 확인

### 데이터 경로 변경

`config.yaml`에서 `storage.data_dir`을 원하는 경로로 변경:

```yaml
storage:
  data_dir: "D:\\MyMonitorData"
```

> 기존 데이터를 유지하려면 이전 폴더의 내용을 새 경로로 복사한 후 변경하세요.

### 로그 확인

문제 발생 시 로그 파일에서 원인을 확인할 수 있습니다:

- **앱 로그:** `C:\OfficeMonitor\app.log`
- **크래시 로그:** `C:\OfficeMonitor\crash.log`

---

## 부록: config.yaml 전체 예시

```yaml
camera:
  id: 0
  resolution: [1280, 720]
  backend: "dshow"
  fallback_ids: [2, 3]

detection:
  model: "buffalo_l"
  det_size: [640, 640]
  interval_ms: 200
  score_threshold: 0.35
  similarity_threshold: 0.65
  min_consecutive_frames: 3
  cooldown_seconds: 300
  auto_augment_embeddings: true

recording:
  codec: "XVID"
  fps: 15
  segment_minutes: 30
  auto_start: false

storage:
  data_dir: "C:\\OfficeMonitor"
  retention_days: 3
  cleanup_interval_hours: 1

shortcuts:
  capture: "Ctrl+Shift+C"
  record_toggle: "Ctrl+R"
  record_pause: "Ctrl+P"
```
